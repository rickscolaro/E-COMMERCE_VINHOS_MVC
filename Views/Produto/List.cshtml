@* @model IEnumerable<Projeto.Models.Produto> *@
@model ProdutoListViewModel

<div>
    <h2>@Model.CategoriaAtual</h2>
</div>

<div class="container ">

    <div class="row">
        @foreach (var item in Model.Produtos)
        {
           <partial name="_ProdutoResumo" for="@item"/>// Referenciando a partial view
        }
    </div>

</div>
